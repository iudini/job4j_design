jps определил pid

jhsdb jmap --heap --pid <pid> показал какой gc используется, с какими настройками и состояние хипа

jstat -gc отслеживал изменения во всех областях, survivor space eden old generation,
так же сборки young и full gc, и время на сборки

для выхода за пределы памяти, уменьшил размер хипа, при полном заполнении gc начал непрерывно делать full gc,
после чего вывалился в OutOfMemoryError